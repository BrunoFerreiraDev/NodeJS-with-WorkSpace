
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Region {
  SUL
  SULDESTE
  NORTE
  NORDESTE
  CENTROOESTE
}

model User {
  id        Int      @id @default(autoincrement())
  name      String
  country   String
  email     String?
  birthDate DateTime
  phone     String?
  region    Region
}